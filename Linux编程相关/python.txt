3.python
	对于python, 需要安装python软件包
	可以用用"python *.py"命令来运行.py文件
	编译命令: python -m py_compile fileA.py fileB.py fileC.py ...
    python用法:
	Python

   Python 语言解释器。
   更多信息：https://www.python.org。

   - 启动 REPL（交互式 shell）：
     python

   - 执行特定的 Python 文件：
     python path/to/file.py

   - 执行特定的 Python 文件并启动 REPL：
     python -i path/to/file.py

   - 执行一个 Python 表达式：
     python -c "expression"

   - 运行指定库模块的脚本：
     python -m module arguments

   - 使用 `pip` 安装包：
     python -m pip install package_name

   - 交互式调试 Python 脚本：
     python -m pdb path/to/file.py

   - 在当前目录的 8000 端口启动内置 HTTP 服务器：
     python -m http.server

	 +++++++++++++++++++++++用法部分选自tldr, 以下为英文原文+++++++++++++++++++++++++++

	  python

  Python language interpreter.
  More information: https://www.python.org.

  - Start a REPL (interactive shell):
    python

  - Execute a specific Python file:
    python path/to/file.py

  - Execute a specific Python file and start a REPL:
    python -i path/to/file.py

  - Execute a Python expression:
    python -c "expression"

  - Run the script of the specified library module:
    python -m module arguments

  - Install a package using `pip`:
    python -m pip install package_name

  - Interactively debug a Python script:
    python -m pdb path/to/file.py

  - Start the built-in HTTP server on port 8000 in the current directory:
    python -m http.server

	+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

	欢迎大家批评指正
